FROM node:22-alpine

WORKDIR /app

# install deps separately for caching
COPY package*.json ./
RUN npm ci

# copy the rest
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev", "--", "-p", "3000", "-H", "0.0.0.0"]
